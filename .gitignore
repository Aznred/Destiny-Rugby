# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# variables d'environnement locales
.env
.env.local
.env.*.local

# ── MODÈLES 3D BRUTS — hors du dépôt ───────────────────────────────────────
# Les .glb livrés (~90 Mo pièce, 3,7 Go) vivent dans un atelier voisin,
# `../destiny-rugby-assets-bruts/` : voir son LISEZ-MOI. Le jeu ne lit que
# `public/m3d/` (114 Mo), produit par `node scripts/copierTrophees.cjs`.
#
# ⚠️ Ces motifs ne suffisent pas à eux seuls : un fichier DÉJÀ suivi le reste
# malgré .gitignore. Ils ont été retirés de l'index avec `git rm --cached`.
sources/modeles/
new model boutique/
